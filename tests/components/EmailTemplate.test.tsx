import React from 'react';
import { render } from '@testing-library/react';
import EmailTemplate from '../../../src/components/EmailTemplate';

test('renders email template component', () => {
  render(<EmailTemplate />);
  // Add assertions here
});